##############################
## Docker configurations
##############################
container:
  provider:
    type: docker

database:
  provider:
    type: sqlite
    path: data/db/ntcore.db

monitoring:
  # provider:
  #   type: cloudwatch
  #   config: 
  #     region: 'cn-northwest-1'
  #     accessKeyId:
  #     secretAccessKey:
  provider:
    type: timescale
    config:
      host: timescaleDB
      port: 5432,
      user: postgres
      database: postgres
      password: password