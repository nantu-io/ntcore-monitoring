##############################
## Docker configurations
##############################
container:
  provider:
    type: docker

database:
  provider:
    type: sqlite
    path: data/db/ntcore.db

timeseries_database:
  provider:
    type: timescale
    config: 
      host: localhost
      port: 5432
      user: postgres
      database: postgres
      password: password