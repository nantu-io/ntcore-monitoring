##############################
## Local configurations
##############################
container:
  provider:
    type: docker

database:
  provider:
    type: sqlite
    config:
      path: data/db/ntcore.db

storage:
  provider:
    type: volume
    config:
      root: data/binary

## This is a temporary config before user auth is onboarded
account: 
  username: ntcore